language: python
env:
  - TOXENV=py27
  - TOXENV=pypy
before_install:
  - pip install twisted
  - pip install epsilon
install:
  - pip install tox
  - pip install coveralls
script:
  - tox
after_success:
  - coveralls
